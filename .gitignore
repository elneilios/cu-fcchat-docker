# Ignore all contents under backups and phpbb but keep placeholder files
/backups/**
!/backups/.gitkeep

/phpbb/**
!/phpbb/.gitkeep

# Note: If files under `phpbb/` are already tracked by git, they will remain tracked
# until you remove them from the index (e.g. `git rm --cached -r phpbb`), then commit.